# 京东夺宝搜索增强

为京东夺宝列表页（[1paipai.jd.com/auction-list](https://1paipai.jd.com/auction-list/)）增加商品关键词搜索的浏览器插件。输入关键词即可**跨页聚合、实时过滤**夺宝商品，无需手动翻页查找。

## 功能简介

| 功能 | 说明 |
|------|------|
| 关键词搜索 | 输入商品名称或 ID，实时过滤夺宝商品 |
| 跨页聚合 | 自动汇总多页商品，搜索结果覆盖全部分页 |
| 自动兜底 | 接口不可用时自动从页面提取商品数据 |
| 多语言 | 支持简体 / 繁体中文 |

## 安装

### 1. 准备图标（可选）
在 `icons/` 目录放入 3 个 PNG 图标：`icon16.png`、`icon48.png`、`icon128.png`。缺少图标也不影响加载。

### 2. 加载到浏览器
- **Chrome / Edge**：打开 `chrome://extensions/` → 开启「开发者模式」→ 点击「加载已解压的扩展程序」→ 选择本项目文件夹。
- **Firefox**：打开 `about:debugging#/runtime/this-firefox` → 点击「临时加载附加组件」→ 选择 `manifest.json`。

## 使用说明

1. 打开京东夺宝列表页，页面顶部会自动出现红色搜索栏。
2. 在搜索框中输入关键词（如「手机」「华为」），结果会**实时**刷新。
3. 搜索结果以结果面板覆盖展示，已跨页汇总所有匹配商品。
4. 点击任意商品卡片，将在**新标签页**打开该商品详情页。
5. 点击搜索框右侧的 **×** 按钮即可**清空搜索**，恢复原始商品列表。
6. 若无匹配结果，面板会显示「未找到匹配商品」的空状态提示。

> 提示：搜索时原生列表会自动隐藏，清空后自动恢复；整个过程无需刷新页面。

## 打包发布

```bash
npm install
npm run build
```

将在项目根目录生成 `jd-auction-search-v1.5.3.zip`，可直接发布。

可选构建参数：
- `node build.js --tw`：以繁体中文（zh-TW）命名输出 `jd-auction-search-v1.5.3-zh-TW.zip`
- `node build.js --firefox`：对 `messages.json` 做 Firefox 字符串转义（`'` → `\'`、`\` → `\\`）
- `node build.js --no-preview`：跳过构建产物预览

## 常见问题

| 情况 | 处理 |
|------|------|
| 页面加载后插件无响应 | 刷新页面，或等待约 2 秒自动加载 |
| 搜索无结果 / 接口失败 | 插件会自动降级为页面数据提取模式 |
| 跨域拦截 | 已配置 `host_permissions`，通常无需处理 |

## 许可证

MIT License
