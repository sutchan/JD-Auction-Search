// 集成测试：jsdom 环境下覆盖依赖真实 DOM 的模块 // - dom/extract.js: extractProductsFromDOM 字段提取（规范字段映射、未命中告警） // - content/search.js: _applyFilterAndUpdate 的 searchMode 分支（隐藏原生、渲染面板、超量分页） // - ui/products.js: 分页渲染 _appendPage（首屏 60 + 加载更多） // 运行：npm test 会调用 .func.test.js 与本文（node integration.test.js）  const fs = require('fs'); const path = require('path'); const vm = require('vm'); const { JSDOM } = require('jsdom');  const ROOT = __dirname; const manifest = JSON.parse(fs.readFileSync(path.join(ROOT, 'manifest.json'), 'utf8')); // 仅加载无头可运行的逻辑+DOM 模块（排除 content.js / background.js 入口） const files = manifest.content_scripts[0].js.filter(f => f !== 'src/content.js' && f !== 'background.js');  let fail = 0; function ok(name, cond) {   if (cond) console.log('  PASS', name);   else { console.log('  FAIL', name); fail++; } }  // 用 jsdom 内部 VM 上下文作为全局，使 window.* 属性可作为裸标识符（如 JDSUtils）解析，等同浏览器 const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', {   url: 'https://1paipai.jd.com/auction-list',   runScripts: 'outside-only' }); const ctx = dom.getInternalVMContext(); // 最小 chrome 桩：getMessage 返回空以走 i18n 硬编码兜底 const chromeStub = new Proxy({}, { get: () => (() => '') }); vm.runInContext('window.chrome = this.chrome; window.requestAnimationFrame = (cb)=>setTimeout(cb,0);',   ctx); // 通过 vm 全局对象注入 chrome 桩（getInternalVMContext 的全局即 window） ctx.chrome = chromeStub;  for (const f of files) {   const code = fs.readFileSync(path.join(ROOT, f), 'utf8');   try { vm.runInContext(code, ctx, { filename: f }); }   catch (e) { console.log('  LOAD ERROR', f, e.message); fail++; } }  const U = vm.runInContext('window.JDSUtils', ctx); const D = vm.runInContext('window.JDSDom', ctx); const UI = vm.runInContext('window.JDSUI', ctx); const document = vm.runInContext('window.document', ctx);  console.log('\n[dom extract] 字段提取与规范映射'); // 构造一个仿真京东拍拍卡片：product-name / price / origin-price / bid-count / img / a document.body.innerHTML = `   <div class="goods-list">     <div class="auction-item">       <a href="https://1paipai.jd.com/auction-detail/123456" title="测试商品A">         <img class="jds-img" src="https://img.jd.com/a.jpg" />         <div class="product-name">测试商品A</div>         <div class="price-current">128.00</div>         <div class="origin-price">238.00</div>         <div class="bid-count">3人出价</div>       </a>     </div>   </div>`; let prods = D.extractProductsFromDOM(); ok('提取到 1 个商品', prods.length === 1); ok('名称提取正确', prods[0] && prods[0].name === '测试商品A'); ok('有出价→currentPrice 映射', prods[0] && prods[0].currentPrice === 128); ok('划线原价→cappedPrice 映射', prods[0] && prods[0].cappedPrice === 238); ok('出价人数→recordCount 映射', prods[0] && prods[0].recordCount === 3); ok('详情链接保留', prods[0] && prods[0].url.includes('123456'));  // getProductPriceText：按名称回查页面原生卡片 .p-price 实际文本（价格只显示 p-price） document.body.innerHTML = `   <div class="goods-list">     <div class="auction-item">       <div class="product-name">价格回查商品</div>       <span class="p-price"><span>¥</span>1,288<span>.00</span></span>     </div>   </div>`; ok('getProductPriceText 命中 p-price 文本', D.getProductPriceText('价格回查商品') === '¥1,288.00'); ok('getProductPriceText 无匹配返回 null', D.getProductPriceText('不存在的商品XYZ') === null);  // 非商品项（分类导航/标签：有名称但无详情链接也无主图）不应混入结果 document.body.innerHTML = `   <div class="goods-list">     <div class="auction-item">       <a href="https://1paipai.jd.com/auction-detail/999001" title="真商品">         <img src="https://img.jd.com/real.jpg" />         <div class="product-name">真商品手机</div>       </a>     </div>     <div class="auction-item">       <div class="name">手机数码分类</div>     </div>     <div class="auction-item">       <div class="name">家用电器标签</div>     </div>   </div>`; let mixed = D.extractProductsFromDOM(); ok('过滤非商品：仅提取到 1 个真实商品', mixed.length === 1 && mixed[0].name === '真商品手机');  console.log('\n[dom extract] 未命中显式告警'); document.body.innerHTML = '<div class="unknown-page">无商品</div>'; let warned = false; const origToast = U.showToast; U.showToast = (k) => { if (k === 'toastDomExtractFailed') warned = true; }; let empty = D.extractProductsFromDOM(); U.showToast = origToast; ok('无卡片时返回空数组', Array.isArray(empty) && empty.length === 0); ok('无卡片时触发 toastDomExtractFailed 告警', warned);  console.log('\n[search] searchMode 编排分支'); const enhancer = vm.runInContext('window.JDSContent.AuctionSearchEnhancer', ctx); enhancer.state = { products: [], filteredProducts: [], keyword: '', isLoading: false, searchMode: false }; enhancer.state.products = [   { id: '1', name: '华为手机', currentPrice: 100, cappedPrice: 200, recordCount: 2 },   { id: '2', name: '苹果手机', currentPrice: 200, cappedPrice: 300, recordCount: 5 } ]; enhancer.state.keyword = '手机'; enhancer._applyFilterAndUpdate(); ok('进入 searchMode', enhancer.state.searchMode === true); ok('过滤命中 2 条', enhancer.state.filteredProducts.length === 2);  // 构造原生列表容器与卡片，验证"搜索隐藏→清空恢复"不破坏原生列表可见性 const listWrap = document.createElement('div'); listWrap.className = 'goods-list'; listWrap.innerHTML = `   <div class="auction-item"><div class="product-name">原生商品X</div></div>   <div class="auction-item"><div class="product-name">原生商品Y</div></div>`; document.body.appendChild(listWrap);  // 模拟搜索态：原生列表应被隐藏 enhancer.state.keyword = '华为'; enhancer._applyFilterAndUpdate(); ok('搜索态隐藏原生列表容器', listWrap.style.display === 'none');  // 清空搜索：原生列表应恢复可见 enhancer.state.keyword = ''; enhancer._applyFilterAndUpdate(); ok('清空后退出 searchMode', enhancer.state.searchMode === false); ok('清空后恢复原生列表容器显示', listWrap.style.display === '');  console.log('\n[products] 分页渲染'); const host = document.createElement('div'); host.id = 'jds-results-host'; document.body.appendChild(host); UI.resultsRoot = host; const panel = document.createElement('div'); panel.className = 'jds-results-panel'; host.appendChild(panel); UI.gridElement = null; const many = Array.from({ length: 130 }, (_, i) => ({ id: 'p' + i, name: '商品' + i, currentPrice: i })); UI.renderProducts(many); const grid = panel.querySelector('.jds-product-grid'); ok('首屏渲染 60 张卡片', grid.children.length === 60); let loadMore = panel.querySelector('.jds-load-more'); ok('出现加载更多按钮', !!loadMore); loadMore.click(); ok('点击后累计 120 张', grid.children.length === 120); loadMore = panel.querySelector('.jds-load-more'); loadMore.click(); ok('再次点击累计 130 张', grid.children.length === 130); ok('全部渲染后无加载更多按钮', !panel.querySelector('.jds-load-more'));  console.log('\n=== ' + (fail === 0 ? 'ALL PASS' : fail + ' FAILED') + ' ==='); process.exit(fail === 0 ? 0 : 1);